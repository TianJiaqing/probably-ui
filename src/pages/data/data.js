const code = {
    tools: [
        "const list = [1, 2, 3]\nconst res = checkType(list, info)\nwindow._DiaLog({ title: '提示', text: `checkType(list, ${ info }) = ${ res }` }).then(res => {}).catch()"
    ]
}
export default code