const code = {
    tools: [
        "const list = [1, 2, 3]\nconst res = checkType(list, info)\nwindow._DiaLog({ _title: '提示', _text: `checkType(list, ${ info }) = ${ res }` }).then(res => {}).catch()"
    ]
}
export default code